import React from 'react';
import cl from './Loader.module.css';
const Loader = (props) => {
    return (
        <div className={cl.loader} {...props}>
            
        </div>
    );
};

export default Loader;